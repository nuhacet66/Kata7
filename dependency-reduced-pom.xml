<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>es.ulpgc</groupId>
  <artifactId>MoneycalculatorMVC</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${project.groupId}.${project.artifactId}.app.Main</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.0.0</version>
        <executions>
          <execution>
            <phase>install</phase>
            <goals>
              <goal>exec</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <executable>jpackage</executable>
          <arguments>
            <argument>--input</argument>
            <argument>target/</argument>
            <argument>--name</argument>
            <argument>${project.artifactId}</argument>
            <argument>--main-jar</argument>
            <argument>${project.artifactId}.jar</argument>
            <argument>--main-class</argument>
            <argument>${project.groupId}.Main</argument>
            <argument>--type</argument>
            <argument>dmg</argument>
            <argument>--icon</argument>
            <argument>src/main/resources/ball.icns</argument>
            <argument>--app-version</argument>
            <argument>${project.version}</argument>
            <argument>--vendor</argument>
            <argument>Your Vendor Name</argument>
            <argument>--mac-package-identifier</argument>
            <argument>${project.groupId}.${project.artifactId}</argument>
            <argument>--mac-package-name</argument>
            <argument>${project.artifactId}</argument>
            <argument>--mac-sign</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <properties>
    <maven.compiler.target>23</maven.compiler.target>
    <project.groupId>es.ulpgc</project.groupId>
    <maven.compiler.source>23</maven.compiler.source>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.artifactId>kata7</project.artifactId>
    <project.version>1.0</project.version>
  </properties>
</project>
